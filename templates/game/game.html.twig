{% extends "base.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block content %}

<div class="container">

    <div class="row mt-5 col-md-12">
        <div class="col-sm-12 offset-8">
            <a href="{{ url("game-doc") }}">Game doc</a> |
            <a href="{{ url("game-play") }}">Start</a>
        </div>
    </div>

    <div class="row mt-5 col-sm-12">
        {{ include('game/rules.markdown.twig')|markdown_to_html }}
    </div>

</div>

{% endblock %}