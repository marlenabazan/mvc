{% extends "base.html.twig" %}

{% block title %}{{ title }}{% endblock %}

{% block content %}

<div class="container">
    <div class="row mt-5 col-md-12">
        <div class="col-sm-3 offset-5">
            <a href="{{ url("deck") }}">Deck</a><br>
            <a href="{{ url("shuffle") }}">Shuffle</a><br>
            <a href="{{ url("draw") }}">Draw</a><br>
            <a href="{{ url("deck2") }}">Deck2</a><br>
            <a href="{{ url("deck-json") }}">DeckJson</a>
        </div>
    </div>
</div>

{% endblock %}
